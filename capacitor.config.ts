import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sensiblsound.app',
  appName: 'Sensible Soundlabs',
  webDir: 'dist'
};

export default config;
